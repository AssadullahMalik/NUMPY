import numpy as np

# Random values
print("Random:", np.random.rand(3))
print("Random Int:", np.random.randint(1, 10, size=5))
print("Normal Distribution:", np.random.randn(3))

# Seed (for reproducibility)
np.random.seed(42)
print("Seeded Random:", np.random.rand(3))
